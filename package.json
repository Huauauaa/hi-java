{
  "name": "hi-java",
  "version": "1.0.0",
  "private": true,
  "description": "Java learning notes — Hugo documentation site",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm build:theme && pnpm --filter @hi-java/site dev",
    "build": "pnpm build:theme && pnpm --filter @hi-java/site build",
    "build:theme": "npm install --prefix packages/hugo-geekdoc && npm exec --prefix packages/hugo-geekdoc -- gulp default",
    "build:site": "pnpm --filter @hi-java/site build"
  },
  "author": "Harvey",
  "license": "MIT"
}
